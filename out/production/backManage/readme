
开发权限管理系统..

-- 登录方式中
--查询所有的一级权限  放入缓存
--根据一级查询所有的二级权限 放入缓存
--查询所有的权限 放入缓存

--登录拦截中
--如果session为空进入登录
--如果不为空，则不处理

-- 权限验证拦截中
-- 从缓存中拿到所有的权限的路径
-- 判断路径和当前请求地址的是否相同，相同则不处理
-- 不相同则拦截，进入到没有权限的页面

-- 不需要控制的路径再，权限验证拦截的路径中使用unless过滤掉

-- 按钮级 权限，自定义标签处理
